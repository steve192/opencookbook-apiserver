<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
    Spring's own console pattern, with %m wrapped so that a newline in anything a caller sent
    cannot end the entry it sits in and forge another. E-mail addresses, recipe titles and the
    Accept-Language header all reach the log, so this is done once here rather than at every
    call site. Lives here and not in application.yml, where Spring would resolve the ${x:-y}
    below as its own placeholders and mangle the format.
    -->
    <property name="MESSAGE" value="%replace(%m){'[\r\n]+', ' '}"/>
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}){} %clr(${PID:-}){magenta} %clr(--- %esb(){APPLICATION_NAME}%esb{APPLICATION_GROUP}[%15.15t] ${LOG_CORRELATION_PATTERN:-}){faint}%clr(%-40.40logger{39}){cyan} %clr(:){faint} ${MESSAGE}%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- Console only: this runs in a container and nothing configures a log file. -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
