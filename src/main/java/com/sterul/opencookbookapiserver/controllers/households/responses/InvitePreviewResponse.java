package com.sterul.opencookbookapiserver.controllers.households.responses;

/** The name and nothing else, so a leaked link reveals nothing more. */
public record InvitePreviewResponse(String householdName) {
}
