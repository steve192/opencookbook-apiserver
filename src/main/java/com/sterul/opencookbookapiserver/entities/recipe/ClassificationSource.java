package com.sterul.opencookbookapiserver.entities.recipe;

/** Who decided a recipe's value of a kind. A run never overwrites what a person chose. */
public enum ClassificationSource {
    USER, DERIVED
}
