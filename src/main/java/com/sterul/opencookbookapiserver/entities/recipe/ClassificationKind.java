package com.sterul.opencookbookapiserver.entities.recipe;

/** What about a recipe a run can derive. Each kind has an attribute that reads and writes it on the recipe. */
public enum ClassificationKind {
    DIET
}
